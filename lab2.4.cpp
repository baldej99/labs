#include <iostream>

using namespace std;
#define sum(a, b) ((a) + (b))
#include "Static.h";
using namespace MySpace;
int main()
{
    int a;
    cin >> a;
    func1(a);
    int b;
    cin >> b;
    func1(b);
}

